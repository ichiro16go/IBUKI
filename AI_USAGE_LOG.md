# AI 活用ログ — Team 06

審査項目「AI 活用度」の根拠資料。Day1〜Day3 を通して、AI ツールをどう使ったかを記録する。
**最低 1 日 3 件以上**の記入を目安に。

## 記入ルール

- 開発の節目ごとに追記する（5 分単位で書くより、機能/論点単位で書く）
- プロンプトは「実際に投げた文」を残す（要約は審査時に減点要因になりうる）
- 出力評価は **採用 / 一部採用 / 不採用** の 3 値で正直に
- 「採用しなかった理由」も書くと AI 活用の判断力アピールになる

---

## Day 1（2026-05-24）

### #001 アイデアの具体化と批判的検証

- **時刻**：12:00~13:00
- **ツール**：Gemini CLI (Interactive Agent), Claude Code CLI
- **目的**：提示された「すれ違い通信」というアイデアに対し、技術的挑戦（ASDD）とメルカリらしさを加味したブラッシュアップを行う。
- **プロンプト**：

  ```text
  まずはアイディアのブラッシュアップをしたいです。
  私のアイディアもありますが、あなたはどういうアプリ案がありますか
  ```

- **出力サマリ**：エージェント介在型すれ違い通信、偏愛の異種間マッピング、反証的対話ファシリテーターの3案を提示。特に「エージェント同士の自律対話（Thinking）」をコアにする方向性を提案。
- **評価**：採用
- **採用 / 不採用の理由**：単なるマッチングではなく、AIが「思考の負債」を肩代わりするというASDDのコンセプトと合致したため。

### #002 体験ロードマップの作成と課題解決

- **時刻**：13:00~13:30
- **ツール**：Gemini CLI
- **目的**：「すれ違い」から「熱狂」に至るまでのステップを整理し、外部フィードバック（ランダム性の妥当性、ネットワーク効果）への論理的な回答を作成する。
- **プロンプト**：

  ```text
  今出た情報から、スタートを「すれ違い通信によるランダム性のある出会い」からゴールの「異趣味間の熱狂」に行くまでのロードマップを書いて、それをみて、どこが不足しているかを見たいです。
  また他の人からのフィードバックで言われたのは
  ー本当にランダムな趣味であはまるか
  ーネットワーク効果が期待できるか
  ```

- **出力サマリ**：5段階のロードマップを提示。ランダム性については「AIによる構造の抽象化（表層ではなく構造で繋ぐ）」、ネットワーク効果については「非同期の残留思念とAI NPCによる補完」という解決策を提示。
- **評価**：採用
- **採用 / 不採用の理由**：こじつけになりがちなランダムな出会いを「構造の類似性」としてAIが翻訳するという視点が、プロダクトの独自性を強めると判断したため。

### #003 ゲーム要素の統合と実装可能性の検証

- **時刻**：13:30~14:15
- **ツール**：Gemini CLI
- **目的**：RPG要素（ステータス化、レイドバトル）を導入する際のAIの役割と、3日間での実装可能性を検討する。
- **プロンプト**：

  ```text
  すれ違い通信の元ネタに倣って、ゲーム系の機能を取り入れてもいいんじゃね？と思います
  例えば
  毎日ランダムな敵が現れて、その日に触れた趣味の数（AIによって図る）などをすることでゲームステータスに作用する。稀にpockemon goのようにリアルで特定の趣味に触れた人に向けてレイドバトルが行われることでリアルのコミュニケーションを醸成していく、みたいなことを考えていました
  ```

- **出力サマリ**：偏愛の「属性・ステータス化」と、文脈（場所等）をボスにする設計を提案。同時に、開発工数の観点から「テキストベースのナラティブ生成」に注力すべきという批判的アドバイスを提示。
- **評価**：一部採用
- **採用 / 不採用の理由**：ゲーム性は採用するが、実装工数を考慮して「リッチなアクション」ではなく「AI生成の物語体験」にリソースを集中するという戦略的判断を採用。

### #004 デザインモックアップの生成

- **時刻**：16:00〜17:00
- **ツール**：Claude Design（@yuseiwnl 担当）
- **目的**：アイデア議論で固まったコンセプトを視覚化し、チーム内の認識合わせとデザイン方針の確定を行う。
- **プロンプト**：

  ```text
  （要旨）すれ違い通信×偏愛カード×植物育成のコンセプトに基づいたモバイルアプリのUIモックを作成してほしい。
  ミニマルで、趣味の温かみを感じるデザインで。
  ```

- **出力サマリ**：メイン画面・すれ違いカードフィード・プロフィール画面のモックを複数案生成。アースカラーベースのデザインシステム（Ibuki デザインガイドライン）の原型となった。
- **評価**：採用
- **採用 / 不採用の理由**：生成されたビジュアル方向性（温かみのあるアース系カラー、カード UI）がプロダクトコンセプトと合致。実装時の `IbukiColors` / `IbukiFonts` の設計指針として採用。

---

## Day 2（2026-05-25）

### #005 BLE vs GPS — すれ違い検出方式の技術調査と意思決定

- **時刻**：10:00〜12:00
- **ツール**：Claude Code（@Kanahe1800 中心にペアプロ）
- **目的**：すれ違い通信の中核となる近接検出の実装方式として BLE・GPS それぞれのトレードオフを整理し、採用方針を決定する。
- **プロンプト**：

  ```text
  すれ違い通信アプリのコア機能として、近接するユーザーを検出したい。
  BLE と GPS それぞれについて、モバイルアプリでの実装難易度・精度・バッテリー消費・距離設定の柔軟性を比較してほしい。
  このアプリのコンセプト（偶然性・軽量動作）を踏まえてどちらを推奨するか意見も聞かせて。
  ```

- **出力サマリ**：
  - BLE：実装難易度高・OS権限取得が複雑・距離設定不可・アプリロジックは軽量
  - GPS：実装比較的容易・距離設定可能・ロジックが重くバッテリー消費増の懸念あり
  - 推奨：「MVP段階ではGPSで距離閾値を設定して実装し、将来的にBLEへの切り替えを検討」
- **評価**：採用
- **採用 / 不採用の理由**：ハッカソン3日間でのデモ実現可能性を最優先し、GPS方式を採用。`detection_method` フィールドをDBに持たせることで将来的なBLE切り替えに対応できる設計にした。

### #006 Supabase テーブル設計 — planter / encounter 関連スキーマ

- **時刻**：12:00〜14:00
- **ツール**：Claude Code（ペアプロ）
- **目的**：すれ違いデータ・植物育成データを永続化するための Supabase テーブル構造と RLS ポリシーを設計する。
- **プロンプト**：

  ```text
  以下のデータを管理するためのSupabaseスキーマを設計してほしい。
  - ユーザー同士のすれ違いイベント（encounters）
  - すれ違い時に交換されるカード（encounter_cards）
  - ユーザーが植えたsukiカードの育成記録（planter_items）
  RLSポリシーも含めて migration SQL を出力して。
  ```

- **出力サマリ**：`encounters` / `encounter_cards` / `planter_items` テーブルの DDL と RLS ポリシーを生成。FK 制約・インデックス設計も含む migration SQL として出力された。
- **評価**：採用
- **採用 / 不採用の理由**：提案されたスキーマがそのまま `supabase/migrations/20260525090606_create_planter_tables.sql` として採用された。

### #007 PlantVisual コンポーネント実装

- **時刻**：14:00〜16:00
- **ツール**：Claude Code（ペアプロ）
- **目的**：sukiカードの成長ステージ（seed / sprout / young / bloom / full）を視覚的に表現するコンポーネントを実装する。
- **プロンプト**：

  ```text
  React Native で植物の成長ステージを表示するコンポーネントを作ってほしい。
  ステージは seed / sprout / young / bloom / full の5段階。
  各ステージに対応する画像ファイル（suki_phase1.png〜suki_phase5.png）を切り替えて表示する。
  アニメーションはシンプルに。
  ```

- **出力サマリ**：`PlantVisual` コンポーネントを生成。`stage` prop に応じた画像マッピングと、ステージ遷移時のフェードアニメーションを実装。
- **評価**：採用
- **採用 / 不採用の理由**：生成されたコードをベースに `frontend/src/components/plant-visual.tsx` として採用。画像アセットの差し替えで即座に対応可能な構造になっている。

### #008 AI アクション推薦機能の設計・実装

- **時刻**：16:00〜18:00
- **ツール**：Claude Code（ペアプロ）
- **目的**：ユーザーが植えた suki に対して「次にとるべきアクション」を AI が提案する機能を設計・実装する。
- **プロンプト**：

  ```text
  植えたsukiカードの内容（タイトル・カテゴリ・詳細）をもとに、
  ユーザーが今日・今週・今月にとれる具体的なアクション（3つ程度）をAIに提案させたい。
  FastAPIのエンドポイントとReact Native側のhookを設計してほしい。
  ```

- **出力サマリ**：`backend/src/routers/hobby.py` にアクション推薦エンドポイントを追加、フロント側に `use-action-recommendations.ts` フックを生成。カードの内容をプロンプトに埋め込んで LLM に投げる実装パターンを提案。
- **評価**：採用
- **採用 / 不採用の理由**：ユーザーの「suki を育てる」体験の中核となる機能であり、実装コストも低いため全採用。

### #009 フロントエンド全般の実装補助（ペアプロ）

- **時刻**：10:00〜21:00（断続的）
- **ツール**：Claude Code
- **目的**：encounters / bookmark / profile / planter 各画面の UI 実装・Supabase クエリ・TypeScript 型定義を網羅的に補助。
- **プロンプト**：（各機能単位で都度投げており1つには集約不可）
- **出力サマリ**：各画面のコンポーネント実装・Supabase join クエリ・型安全な fetch 関数を生成。エラーハンドリング・ローディング状態管理のパターンも統一。
- **評価**：採用
- **採用 / 不採用の理由**：Day 2 の実装の大部分において Claude Code をペアプログラマーとして活用。生成コードは都度レビューのうえ採用。型エラー・ロジックバグの指摘も多数あり、品質維持に貢献した。

---

## Day 3（2026-05-26）

### #00X

---

## 全体振り返り

- **AI が一番効いた場面**：
- **AI に頼らなかった場面とその理由**：
- **次回 Hackathon で改善したい AI 活用**：
